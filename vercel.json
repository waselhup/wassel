{
    "version": 2,
    "installCommand": "npm install --legacy-peer-deps",
    "buildCommand": "npx vite build && npx esbuild server/_core/vercel.ts --platform=node --packages=external --bundle --format=cjs --outfile=api/index.js",
    "outputDirectory": "dist/public",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}