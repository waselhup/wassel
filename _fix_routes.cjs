const fs=require('fs');const fp='C:\\Users\\WIN11-24H2GPT\\Desktop\\wassel-v2\\server\\_core\\vercel.ts';let f=fs.readFileSync(fp,'utf8');if(!f.includes('test-route')){f=f.replace("// ===== Gmail OAuth Routes =====","app.get('/api/test-route', (_req, res) => { res.json({ok:true,routes:'working'}); });\n\n// ===== Gmail OAuth Routes =====");fs.writeFileSync(fp,f,'utf8');console.log('Added test route');}else{console.log('Already has test route');}
