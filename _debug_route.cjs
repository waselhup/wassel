const fs=require('fs');const fp='C:\\Users\\WIN11-24H2GPT\\Desktop\\wassel-v2\\server\\_core\\vercel.ts';let f=fs.readFileSync(fp,'utf8');const debugLine="app.use((req, _res, next) => { console.log('REQ:', req.method, req.url, req.path); next(); });\n\n";if(!f.includes('REQ:')){f=f.replace("app.get('/api/health'",debugLine+"app.get('/api/health'");fs.writeFileSync(fp,f,'utf8');console.log('Added debug logging');}else{console.log('Already has debug logging');}
