const fs=require('fs');const fp='C:\\Users\\WIN11-24H2GPT\\Desktop\\wassel-v2\\client\\public\\locales\\en\\translation.json';const b=fs.readFileSync(fp);let s=b.toString('utf8');while(s.length>0&&s.charCodeAt(0)!==123&&s.charCodeAt(0)!==91)s=s.slice(1);console.log('First char after strip:',s.charAt(0),'code:',s.charCodeAt(0));try{const j=JSON.parse(s);console.log('Keys:',Object.keys(j).join(','));console.log('Has profile:',!!j.profile);if(!j.profile)j.profile={};j.profile.gmailConnection='Gmail Connection';j.profile.connectGmail='Connect Gmail for Campaign Sending';j.profile.gmailConnected='Gmail Connected Successfully';j.profile.gmailConnectedDesc='You can now send email campaigns from your account';j.profile.gmailDesc='Connect your Gmail account to send email campaigns directly';fs.writeFileSync(fp,JSON.stringify(j,null,2),'utf8');console.log('EN DONE');}catch(e){console.log('Parse failed:',e.message);console.log('First 200 chars:',s.substring(0,200));}
